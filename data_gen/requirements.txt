# =============================================================================
# SBI Realtime Fraud Detection — Python 패키지 목록
# 검증 환경: Python 3.9.x / RHEL 9.6 (Air-gapped)
#
# RHEL 9.6에는 Python 3.9가 기본 포함 → SCL 설치 불필요
#   python3 --version  # 3.9.x 확인
#
# =============================================================================
# Air-gapped 환경 설치 방법
# =============================================================================
#
# ※ 중요: pip download 는 반드시 클러스터와 동일한 OS/아키텍처인
#          RHEL 9.6 x86_64 Bastion 머신에서 실행해야 합니다.
#          macOS나 다른 OS에서 실행하면 플랫폼 불일치로 패키지를 찾지 못합니다.
#
# [Step 1] gssapi / Kerberos 시스템 패키지 설치 (dnf/yum 내부 저장소 사용)
#          클러스터 노드 및 Bastion 머신 모두에 설치
#   sudo yum install -y gcc python3-devel krb5-devel python3-gssapi
#
# [Step 2] RHEL 9.6 Bastion 머신에서 (인터넷 연결 필요):
#   python3 -m venv --system-site-packages /tmp/sbi-venv
#   source /tmp/sbi-venv/bin/activate
#   pip install --upgrade pip
#   pip download -r requirements.txt -d ./wheels/
#   tar cf sbi-wheels.tar wheels/
#   scp sbi-wheels.tar systest@<클러스터-호스트>:/tmp/
#
# [Step 3] 클러스터 노드에서 오프라인 설치:
#   sudo yum install -y gcc python3-devel krb5-devel python3-gssapi
#   python3 -m venv --system-site-packages /tmp/sbi-venv
#   source /tmp/sbi-venv/bin/activate
#   cd /tmp && tar xf sbi-wheels.tar
#   pip install --no-index --find-links=./wheels/ -r requirements.txt
#
# [Step 4] 설치 확인:
#   python3 -c "import gssapi, kafka, sdv, pandas, numpy; print('All OK')"
#
# =============================================================================

sdv>=1.9.0
pandas>=1.5.0
numpy>=1.23.0
kafka-python>=2.0.0
python-snappy>=0.7.0
