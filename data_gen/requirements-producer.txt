# =============================================================================
# SBI Fraud Detection — kafka_producer.py 전용 최소 패키지 목록 (--input 모드)
# 대상: RHEL 9.6 / Python 3.9.x / Air-gapped 클러스터
#
# SDV wheels 반입이 어렵거나 불필요한 경우 사용합니다.
# kafka_producer.py --input <csv> 실행만 가능 (합성 데이터 생성 불가).
# 전체 SDV 기반 데이터 생성이 필요하면 requirements.txt 를 사용하세요.
#
# =============================================================================
# Air-gapped 설치 방법
# =============================================================================
#
# ※ 중요: pip download 는 반드시 클러스터와 동일한 OS/아키텍처인
#          RHEL 9.6 x86_64 Bastion 머신에서 실행해야 합니다.
#
# [Step 1] 시스템 패키지 설치 (Bastion 및 클러스터 노드 모두):
#   sudo yum install -y gcc python3-devel krb5-devel python3-gssapi gettext
#
# [Step 2] RHEL 9.6 Bastion 머신에서 (인터넷 연결 필요):
#   python3 -m venv --system-site-packages /tmp/sbi-venv
#   source /tmp/sbi-venv/bin/activate
#   pip install --upgrade pip
#   pip download -r requirements-producer.txt -d ./wheels-producer/
#   tar cf sbi-wheels-producer.tar wheels-producer/
#   scp sbi-wheels-producer.tar systest@<클러스터-호스트>:/tmp/
#
# [Step 3] 클러스터 노드에서 오프라인 설치:
#   python3 -m venv --system-site-packages /tmp/sbi-venv
#   source /tmp/sbi-venv/bin/activate
#   cd /tmp && tar xf sbi-wheels-producer.tar
#   pip install --no-index --find-links=./wheels-producer/ -r /root/sbi-fraud-detection-pipeline/data_gen/requirements-producer.txt
#
# [Step 4] 설치 확인:
#   python3 -c "import kafka, pandas, numpy, snappy; print('All OK')"
#
# [Step 5] CSV 파일로 Kafka 전송:
#   source config/env.conf
#   python data_gen/kafka_producer.py --input /tmp/txn.csv --rate 100
#
# =============================================================================

kafka-python>=2.0.0
pandas>=1.5.0
numpy>=1.23.0
python-snappy>=0.7.0
