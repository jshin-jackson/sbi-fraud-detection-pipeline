# =============================================================================
# Spark + Iceberg + Ozone(S3A) + Kerberos 설정
# Air-gapped 환경 전용 — 인터넷 연결 없이 로컬 JAR을 사용합니다.
#
# 전제조건: CDP 파슬에 Iceberg 및 Kafka connector JAR이 사전 포함되어 있어야 합니다.
#   Cloudera CDP 7.3.1 기준 JAR 위치:
#     /opt/cloudera/parcels/CDH/jars/iceberg-spark-runtime-*.jar
#     /opt/cloudera/parcels/CDH/jars/spark-sql-kafka-*.jar
#
# 적용 방법 1 — spark-submit:
#   spark-submit --properties-file /path/to/conf/spark_iceberg.conf ...
#
# 적용 방법 2 — spark-defaults.conf에 복사:
#   cp conf/spark_iceberg.conf $SPARK_HOME/conf/spark-defaults.conf
# =============================================================================

# ---------------------------------------------------------------------------
# Air-gapped 환경 — 로컬 JAR 경로 (CDP 파슬 기준)
# CDP 7.3.1 파슬에 Iceberg/Kafka JAR이 사전 패키징되어 있음.
# 실제 파일명은 클러스터 CDP 버전에 따라 확인 필요.
# ---------------------------------------------------------------------------
spark.jars=/opt/cloudera/parcels/CDH/jars/iceberg-spark-runtime-3.3_2.12-1.4.3.jar,\
/opt/cloudera/parcels/CDH/jars/spark-sql-kafka-0-10_2.12-3.3.2.jar,\
/opt/cloudera/parcels/CDH/jars/kafka-clients-3.4.0.jar

# ---------------------------------------------------------------------------
# Iceberg 확장
# ---------------------------------------------------------------------------
spark.sql.extensions=org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions
spark.sql.catalog.spark_catalog=org.apache.iceberg.spark.SparkSessionCatalog
spark.sql.catalog.spark_catalog.type=hive
spark.sql.catalog.spark_catalog.uri=thrift://hiveserver2.sbi.local:9083

# ---------------------------------------------------------------------------
# Ozone S3A 설정
# ---------------------------------------------------------------------------
spark.hadoop.fs.s3a.endpoint=http://ozone-s3g.sbi.local:9878
spark.hadoop.fs.s3a.path.style.access=true
spark.hadoop.fs.s3a.impl=org.apache.hadoop.fs.s3a.S3AFileSystem
spark.hadoop.fs.s3a.connection.ssl.enabled=true
spark.hadoop.fs.s3a.ssl.channel.mode=openssl

# Ozone S3 게이트웨이 인증 (AWS SDK 형식)
# Cloudera CDP에서는 Kerberos 연동 시 아래 값을 비워두거나
# ozone.security.enabled=true 환경에서는 자동으로 Kerberos 토큰을 사용합니다.
spark.hadoop.fs.s3a.aws.credentials.provider=org.apache.hadoop.fs.s3a.SimpleAWSCredentialsProvider
spark.hadoop.fs.s3a.access.key=REPLACE_WITH_OZONE_ACCESS_KEY
spark.hadoop.fs.s3a.secret.key=REPLACE_WITH_OZONE_SECRET_KEY

# S3A 성능 튜닝
spark.hadoop.fs.s3a.block.size=134217728
spark.hadoop.fs.s3a.multipart.size=67108864
spark.hadoop.fs.s3a.fast.upload=true
spark.hadoop.fs.s3a.fast.upload.buffer=bytebuffer
spark.hadoop.fs.s3a.threads.max=20
spark.hadoop.fs.s3a.connection.maximum=20

# ---------------------------------------------------------------------------
# Kerberos 인증
# ---------------------------------------------------------------------------
spark.kerberos.keytab=/etc/security/keytabs/sbi-spark.keytab
spark.kerberos.principal=sbi-spark@SBI.LOCAL
spark.yarn.keytab=/etc/security/keytabs/sbi-spark.keytab
spark.yarn.principal=sbi-spark@SBI.LOCAL

# ---------------------------------------------------------------------------
# Iceberg 쓰기 최적화
# ---------------------------------------------------------------------------
spark.sql.iceberg.merge-on-read.enabled=true
spark.sql.iceberg.handle-timestamp-without-timezone=true

# ---------------------------------------------------------------------------
# Spark SQL 최적화
# ---------------------------------------------------------------------------
spark.sql.adaptive.enabled=true
spark.sql.adaptive.coalescePartitions.enabled=true
spark.sql.adaptive.skewJoin.enabled=true
spark.sql.shuffle.partitions=200

# ---------------------------------------------------------------------------
# Streaming 설정
# ---------------------------------------------------------------------------
# 체크포인트 위치 (raw_ingest_job.py 에서 재정의 가능)
spark.streaming.checkpoint.dir=s3a://sbi-raw/checkpoints/

# ---------------------------------------------------------------------------
# YARN 실행 설정
# ---------------------------------------------------------------------------
spark.master=yarn
spark.submit.deployMode=cluster
spark.executor.instances=4
spark.executor.cores=2
spark.executor.memory=4g
spark.driver.memory=2g
spark.dynamicAllocation.enabled=true
spark.dynamicAllocation.minExecutors=2
spark.dynamicAllocation.maxExecutors=16
